{
  "name": "tritio-monorepo",
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "private": true,
  "scripts": {
    "dev": "bun run --filter playground dev",
    "build": "bun run --filter ./packages/* build",
    "test": "bun test",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "bun run build && changeset publish"
  },
  "type": "module",
  "workspaces": [
    "packages/*",
    "playground"
  ]
}
